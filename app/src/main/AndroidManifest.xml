<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.ufersa.qwater">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:name=".models.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.TabHostActivity"
            android:label="@string/app_name" />
        <activity android:name=".activities.MainActivity" />

        <activity
            android:name=".activities.WaterSamplesDetailsActivity"
            android:noHistory="true" />
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.AboutActivity"
            android:theme="@style/AppTheme.CustomTheme" />

        <activity
            android:name=".activities.PopupGraphActivity"
            android:theme="@style/AppTheme.CustomTheme" />

        <activity
            android:name=".activities.ListWaterSourcesActivity"
            android:label="@string/title_activity_water_source"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".activities.ListWaterSamplesActivity"
            android:label="@string/title_activity_water_sample"
            android:theme="@style/AppTheme.NoActionBar"
            android:noHistory="true" />

        <activity android:name=".activities.CreateWaterSourceActivity"/>

    </application>

</manifest>